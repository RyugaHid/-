import styled from "@emotion/styled"

export const SidebarPersonInfo = styled.div`
  display: flex;
  margin: 20px 0;
  gap: 8px;
  align-items: center;
  border-radius: ${({ theme }) => theme.main?.borderRadius};
`
